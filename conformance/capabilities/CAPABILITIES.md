# Capability inventory

Generated by `python3 conformance/capabilities/manage.py refresh`.

Review date: 2026-09-10. AlphaTab: 1.8.4 (`022a45c8e42370f9e12e68949d11eada370da83d`).

The inventory contains 107 capability rows and 405 mapped format-documentation rows.
The pinned source inventory contains 4365 constructs from 119 files.
There are 50 upstream fixtures without a byte-identical local fixture.

Support ratings describe the scope in each finding. Partial and unverified rows receive no completion credit.
A linked source construct has a capability association, not individual behavior proof.

| Stage | Supported | Partial | Missing | Unverified |
| --- | ---: | ---: | ---: | ---: |
| import | 57 | 22 | 20 | 2 |
| model | 57 | 22 | 21 | 1 |
| export | 38 | 23 | 38 | 2 |

All three stages have a supported rating in 36 rows. This is a checklist count, not a percentage of all musical behavior.

## Runtime probe

The receipt contains 364 input files. Probe freshness against the current source: `false`.
Raw consumer differences require review. Default-only cases do not prove feature support.

| Capability | Files with non-default source values | Files with differences | Blocked comparisons |
| --- | ---: | ---: | ---: |
| Stylesheet, brackets and track-name policies | 364 | 338 | 26 |
| Concert and display transposition | 347 | 322 | 26 |
| System layout and forced line breaks | 250 | 230 | 26 |
| Track short names | 364 | 194 | 26 |
| Beam grouping, direction and stem overrides | 200 | 180 | 26 |
| Authored pitch spelling and accidentals | 157 | 137 | 26 |
| Track visibility and standard/tab flags | 77 | 90 | 26 |
| Brush and arpeggio timing | 21 | 17 | 26 |
| Whammy-bar vibrato strength | 17 | 17 | 26 |
| Separate section marker and text | 31 | 16 | 26 |
| Left and right hand fingering | 13 | 13 | 26 |
| Tremolo picking and stroke styles | 14 | 13 | 26 |
| Double bar lines | 32 | 12 | 26 |
| Extended barline and bar-number controls | 32 | 12 | 26 |
| Tap, slap and pop distinctions | 13 | 12 | 26 |
| Chord diagrams, barres and fingering | 11 | 11 | 26 |
| Pick stroke direction | 11 | 11 | 26 |
| Wah pedal state | 11 | 11 | 26 |
| Slashed beats and slash staff notation | 8 | 8 | 26 |
| Lyrics authored directly on a beat | 5 | 5 | 26 |
| Authored note duration percentage | 6 | 4 | 26 |
| Multiple-bar rest preferences | 4 | 4 | 26 |
| Volume automation events | 8 | 4 | 26 |
| Chord name, diagram and fingering visibility | 3 | 3 | 26 |
| Numbered staff notation | 3 | 3 | 26 |
| Authored timer marks | 2 | 2 | 26 |
| Beat barre fret and shape | 2 | 2 | 26 |
| Dead-slapped beats | 2 | 2 | 26 |
| Golpe thumb and finger marks | 2 | 2 | 26 |
| Explicit note string-number display | 1 | 1 | 26 |
| Fade out and volume swell | 1 | 1 | 26 |
| Key signatures and key mode spelling | 44 | 1 | 26 |
| Pan and balance automation events | 1 | 1 | 26 |
| Rasgueado patterns | 1 | 1 | 26 |
| Sustain pedal markers | 1 | 1 | 26 |
| Turns and mordents | 1 | 1 | 26 |
| Alternate endings | 16 | 0 | 26 |
| Beat octave shifts | 10 | 0 | 26 |
| Common and per-staff capo | 1 | 0 | 26 |
| Fade in | 17 | 0 | 26 |
| Fermata placement, kind and length | 4 | 0 | 26 |
| Free-time bars | 1 | 0 | 26 |
| Left-hand tapping identity | 1 | 0 | 26 |
| Legato and authored slurs | 2 | 0 | 26 |
| MIDI bank selection and bank changes | 8 | 0 | 26 |
| Navigation targets and jumps | 8 | 0 | 26 |
| Note vibrato strength | 21 | 0 | 26 |
| Octave shifts attached to clefs | 2 | 0 | 26 |
| One-bar and two-bar simile marks | 4 | 0 | 26 |
| Repeat starts and displayed pass counts | 28 | 0 | 26 |
| String tuning and tuning labels | 360 | 0 | 26 |
| Tenuto accent | 1 | 0 | 26 |

## Reviewed capabilities

Priority 1 affects core semantics or audit confidence. Priority 2 covers techniques and expressions. Priority 3 covers display details or scope extensions.

### oracle-breadth: Independent oracle breadth and blind spots

Audit. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

The corpus oracle omits many authored fields. Zero current differences only describes its selected projection. Broad probes record additional raw consumer differences.

Completion criterion: Extend independent comparisons for each new public capability with valid non-default fixtures and narrow normalization rules.

Bounded work: [Require executable evidence for supported capability claims](https://github.com/CaliLuke/go-guitar-pro/issues/67); [Emit GP8 archives readable by the pinned AlphaTab inflater](https://github.com/CaliLuke/go-guitar-pro/issues/68).

### upstream-discovery: Complete upstream capability discovery

Audit. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **unverified**. Model: **unverified**. GP8 export: **unverified**.

Old inventory spans nine primary model files, omits related classes/enums and misattributes BeamingRules fields to MasterBar. Expanded lexical inventory leaves unmapped constructs unverified.

Completion criterion: Review every unlinked upstream construct and new documentation row. Preserve explicit scope and independent stage evidence.

Bounded work: [Correct stale semantic documentation and diagnostic labels](https://github.com/CaliLuke/go-guitar-pro/issues/46); [Classify authored GP constructs with importer-backed ownership](https://github.com/CaliLuke/go-guitar-pro/issues/71).

### chord-diagram: Chord diagrams, barres and fingering

Chords. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

Name, string pattern and representable first fret survive. GP8 omits barres, fingerings, omissions and legacy descriptions.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve representable chord diagram barres and fingerings](https://github.com/CaliLuke/go-guitar-pro/issues/79).

### chord-name: Chord names and scoped definitions

Chords. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Track/staff chord definitions retain separate ownership and names.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### chord-display: Chord name, diagram and fingering visibility

Chords. Priority 3. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF diagram display properties are audited but not retained as independent public flags.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve independent chord display flags](https://github.com/CaliLuke/go-guitar-pro/issues/104).

### bends: Bend curves and styles

Expression. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

Common gestures and curves work. Narrow point units, target hold interpretation and missing BendStyle distinctions prevent full parity.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve exact authored bend offsets](https://github.com/CaliLuke/go-guitar-pro/issues/60); [Preserve GPIF bend control roles with nonmonotonic offsets](https://github.com/CaliLuke/go-guitar-pro/issues/62).

### dynamics: Beat dynamics and note velocities

Expression. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Eight GPIF dynamics and binary velocities are modeled. GP8 quantizes the beat and cannot preserve distinct note velocities inside one chord.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Establish target limits for per-note dynamics and velocities](https://github.com/CaliLuke/go-guitar-pro/issues/61).

### grace: Ordered grace notes and transitions

Expression. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

Ordered note-level graces preserve several fields. Full beat-level grace chords, timing/style distinctions and target duration/transition restrictions need further evidence.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Bound remaining grace chord and transition semantics](https://github.com/CaliLuke/go-guitar-pro/issues/65).

### tremolo: Tremolo picking and stroke styles

Expression. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy durations parse. GPIF durations project into legacy note effects. GP8 omits tremolo picking. Extended marks/styles require new model evidence.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Export tremolo picking and preserve supported stroke variants](https://github.com/CaliLuke/go-guitar-pro/issues/57).

### whammy: Whammy curves and styles

Expression. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

Curve normalization and strict loss reports are tested. Target gesture interpretation can remove authored holds and rates.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Apply exact authored offset preservation to whammy curves](https://github.com/CaliLuke/go-guitar-pro/issues/72).

### barre: Beat barre fret and shape

Expression. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

BarreFret and BarreString properties are audited as omissions. Chord diagram barres are a separate concept.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve beat barre fret and shape](https://github.com/CaliLuke/go-guitar-pro/issues/74).

### beat-octave: Beat octave shifts

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

All supported 8va/8vb/15ma/15mb beat shifts survive. These do not cover bar clef shifts.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### beat-vibrato: Whammy-bar vibrato strength

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

GPIF strength collapses into BeatEffects.Vibrato. GP8 omits beat vibrato.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve and export beat vibrato strength](https://github.com/CaliLuke/go-guitar-pro/issues/77).

### brush: Brush and arpeggio timing

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

Direction survives. GPIF brush duration XProperty is not retained. The GP8 writer uses a fixed eighth-note stroke duration.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve authored brush and arpeggio timing](https://github.com/CaliLuke/go-guitar-pro/issues/78).

### dead-slap: Dead-slapped beats

Expression. Priority 2. Formats: gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF DeadSlapped is absent. Existing oracle normalization treats an empty dead slap as a rest, which does not preserve the mark.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve dead-slapped beats without reducing them to rests](https://github.com/CaliLuke/go-guitar-pro/issues/80).

### fade-in: Fade in

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Fade-in is preserved. Fade-out and volume swell require a richer public model.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### fade-other: Fade out and volume swell

Expression. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

Only a FadeIn boolean is exposed. Other GPIF Fadding variants produce lossy-projection diagnostics and disappear.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve fade-out and volume-swell beat effects](https://github.com/CaliLuke/go-guitar-pro/issues/81).

### golpe: Golpe thumb and finger marks

Expression. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF Golpe is absent from wire and public beat types.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve thumb and finger golpe marks](https://github.com/CaliLuke/go-guitar-pro/issues/83).

### hairpins: Crescendo and decrescendo

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Public hairpins have dedicated input and independent GP8 export evidence.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### note-vibrato: Note vibrato strength

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

NoteEffect.VibratoStrength preserves Slight and Wide and is authoritative when nonzero. A true legacy Vibrato value falls back to Slight, while beat-level vibrato remains a separate export omission.

Completion criterion: Keep Slight and Wide distinct through import, typed model edits, GP8 output, and pinned AlphaTab consumption while retaining the documented legacy Slight fallback.

### pick-stroke: Pick stroke direction

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **missing**.

Pick stroke parses into the public model, but GP8 export reports it as omitted.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Export existing pick stroke directions](https://github.com/CaliLuke/go-guitar-pro/issues/88).

### rasgueado: Rasgueado patterns

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy boolean data exists. GPIF named finger patterns do not have a public representation. GP8 omits rasgueado.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve named rasgueado finger patterns](https://github.com/CaliLuke/go-guitar-pro/issues/89).

### tap-slap-pop: Tap, slap and pop distinctions

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Slap and pop share one SlapEffect value and cannot coexist. Separate note Tapped flags survive. GP8 omits the beat SlapEffect.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve independent tap, slap and pop beat techniques](https://github.com/CaliLuke/go-guitar-pro/issues/93).

### wah: Wah pedal state

Expression. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy mix-table Wah data exists. GPIF Beat.Wah is explicitly unsupported and there is no direct beat pedal enum.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve wah pedal state through GPIF and GP8](https://github.com/CaliLuke/go-guitar-pro/issues/94).

### container: GP3-GP8 containers and file APIs

Formats. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Compatibility suite covers all inventoried files. Parser diagnostics and file exports are available. Corpus success does not prove semantic fidelity.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### password: Password-locked Guitar Pro files

Formats. Priority 3. Formats: gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

No decryption support. The format website also lists this as unsupported by AlphaTab.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Decide scope and oracle for password-protected Guitar Pro files](https://github.com/CaliLuke/go-guitar-pro/issues/116).

### absolute-pitch: Non-stringed MIDI pitch

Instrument. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

String 0 represents an absolute MIDI note, with range and independent export checks.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### capo: Common and per-staff capo

Instrument. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Staff.CapoFret preserves independent authored capo values. Track.CapoFret remains a documented first-staff compatibility scalar with deterministic edit precedence.

Completion criterion: Two staves with capos 2 and 5 retain both values and sounding pitches through GP8 and the pinned consumer. Zero, negative, and int32 boundaries plus legacy/direct edit precedence have exact evidence.

Bounded work: [Preserve independent capo values on multiple staves](https://github.com/CaliLuke/go-guitar-pro/issues/43).

### clef: Treble, bass, alto, tenor and percussion clefs

Instrument. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Five supported clefs survive independent export checks. Clef octave shifts are a separate field.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### clef-octave: Octave shifts attached to clefs

Instrument. Priority 1. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Measure.ClefOctave preserves every GPIF Bar.Ottavia value at its staff and measure location independently from Beat.Octave. GP8 export and post-parse edits retain the same values through pinned AlphaTab consumption.

Completion criterion: Keep the four clef-octave values covered across import, public editing, GP8 export, exact wire locations, and pinned AlphaTab consumption.

Bounded work: [Preserve bar-level clef octave shifts](https://github.com/CaliLuke/go-guitar-pro/issues/44).

### percussion: Percussion identities and notation

Instrument. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Separate articulation identities and MIDI output survive. Some notation resources and conflicting staff line counts have explicit target limits. Four existing corpus files fail GP8 export on percussion values without native articulation mappings.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Export valid percussion notes currently rejected by GP8](https://github.com/CaliLuke/go-guitar-pro/issues/53).

### transposition: Concert and display transposition

Instrument. Priority 1. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF Transpose produces an unsupported-feature diagnostic. PartSounding and independent display/sounding pitch offsets have no public staff destination.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve sounding and display transposition separately](https://github.com/CaliLuke/go-guitar-pro/issues/56).

### tuning: String tuning and tuning labels

Instrument. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **supported**.

MIDI tuning values survive. AlphaTab stringTuning also stores authored labels that the public string slice cannot retain.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve tuning labels independently from pitches](https://github.com/CaliLuke/go-guitar-pro/issues/58).

### legacy-instrument: Fret count, twelve strings and banjo

Instrument. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Legacy instrument flags parse. GP8 reports fret count, twelve-string and banjo flags as omissions.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Resolve GP8 support for legacy instrument flags](https://github.com/CaliLuke/go-guitar-pro/issues/97).

### partial-capo: Partial capo across selected strings

Instrument. Priority 3. Formats: gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

No per-string capo model. Website lists this as unsupported in AlphaTab too; keep it separate from parity blockers.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Decide the supported scope for partial capos](https://github.com/CaliLuke/go-guitar-pro/issues/115).

### metadata: Score identity and credits

Metadata. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Most score metadata survives. Binary Writer, Comments, Date and clipboard provenance lack equivalent GP8 output. Author and Writer remain distinct.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Classify GP8 limits for binary-only score metadata](https://github.com/CaliLuke/go-guitar-pro/issues/99).

### short-name: Track short names

Metadata. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF ShortName is absent from the decoded track and public Track.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve authored track short names](https://github.com/CaliLuke/go-guitar-pro/issues/91).

### track-identity: Track name and color

Metadata. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Track name, color and identity have public import and export tests. Short name is a separate gap.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### beaming: Beam grouping, direction and stem overrides

Notation. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Binary display flags exist. GPIF XProperties and stem-direction elements are absent. GP8 omits authored beam groups and beat display overrides.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve authored beam groups and stem overrides](https://github.com/CaliLuke/go-guitar-pro/issues/75).

### notation-visibility: Track visibility and standard/tab flags

Notation. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **supported**.

ZIP import reads LayoutConfiguration visibility but does not read PartConfiguration staff view flags. GP8 writes notation/tab flags from track settings.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Read and preserve per-staff part notation settings](https://github.com/CaliLuke/go-guitar-pro/issues/86).

### slash: Slashed beats and slash staff notation

Notation. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF Slashed and per-staff slash notation lack lossless public destinations.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve slashed beats and slash staff notation separately](https://github.com/CaliLuke/go-guitar-pro/issues/92).

### display-duration-override: Independent display-duration override

Notation. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **unverified**. Model: **missing**. GP8 export: **unverified**.

AlphaTab Beat.overrideDisplayDuration has no equivalent public field. Its applicability to Guitar Pro input needs a fixture; other AlphaTab importers can author it.

Completion criterion: Find a valid Guitar Pro source for an independent display duration or document that this is exclusive to other formats.

Bounded work: [Determine whether GP input authors independent display duration](https://github.com/CaliLuke/go-guitar-pro/issues/111).

### layout: System layout and forced line breaks

Notation. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy line breaks parse. GPIF system layouts and bar/master-bar scale lack public equivalents. Export omits line-break preferences.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve authored system layouts and bar display scales](https://github.com/CaliLuke/go-guitar-pro/issues/112).

### multi-rest: Multiple-bar rest preferences

Notation. Priority 3. Formats: gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

No public score/track multirest display preferences corresponding to the AlphaTab stylesheet.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve score and track multirest preferences](https://github.com/CaliLuke/go-guitar-pro/issues/105).

### note-display: Note visibility and notehead overrides

Notation. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

Pitched-note visibility, custom noteheads and note style are absent. Percussion notehead support is tracked separately.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Determine GP source support for note visibility and noteheads](https://github.com/CaliLuke/go-guitar-pro/issues/113).

### numbered: Numbered staff notation

Notation. Priority 3. Formats: gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

The public staff settings do not represent AlphaTab numbered notation.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve numbered notation per staff](https://github.com/CaliLuke/go-guitar-pro/issues/106).

### page-setup: Page setup, headers and footer templates

Notation. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy PageSetup is modeled and omitted on GP8 export. Pinned AlphaTab also exposes structured score header/footer templates not mapped here.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve supported header and footer templates and visibility](https://github.com/CaliLuke/go-guitar-pro/issues/114).

### show-string: Explicit note string-number display

Notation. Priority 3. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

ShowStringNumber is recognized by the audit but omitted from the public note.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve explicit string-number display requests](https://github.com/CaliLuke/go-guitar-pro/issues/107).

### stylesheet: Stylesheet, brackets and track-name policies

Notation. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy page and track settings partially parse. BinaryStylesheet authored policies, hide-empty staves, brackets and track-name modes lack full round-trip support.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve supported score bracket, track-name and display policies](https://github.com/CaliLuke/go-guitar-pro/issues/117).

### timer: Authored timer marks

Notation. Priority 3. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF Timer and explicit/derived timer visibility have no public destination.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve authored timer marks and visibility](https://github.com/CaliLuke/go-guitar-pro/issues/108).

### legato-slurs: Legato and authored slurs

Notes. Priority 1. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Beat.Legato preserves authored origin and destination endpoints as an occurrence-owned record. No separate note-level slur contract is inferred without source evidence.

Completion criterion: Keep each beat-level endpoint independent through GPIF import, public edits, GP8 output, reimport, and pinned AlphaTab origin and derived-destination consumption.

Bounded work: [Preserve authored legato and slur endpoints](https://github.com/CaliLuke/go-guitar-pro/issues/50).

### ties: Tie markers and origin validation

Notes. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Origin/destination markers and pitch identity have tests. A destination without an earlier origin is diagnosed, while excerpts can still export.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### accent: Normal and heavy accents

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

NoteEffect.Accent preserves normal, heavy, and tenuto values. A nonzero typed value is authoritative; legacy normal and heavy booleans are fallbacks, and a conflict produces a scoped normalization report.

Completion criterion: Keep all three typed accent values exact through GPIF import and GP8 output, preserve the legacy fallback, and report typed-versus-legacy conflicts without changing stage support.

### accidentals: Authored pitch spelling and accidentals

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Pitch is projected to MIDI/fret. Concert/transposed spelling and explicit accidental modes are not lossless. SwapAccidentals is omitted on export.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve authored pitch spelling and accidental choices](https://github.com/CaliLuke/go-guitar-pro/issues/73).

### dead-ghost: Dead notes and ghost notes

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Dead and ghost states have separate public fields and target cases.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### fingering: Left and right hand fingering

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **missing**.

Presence markers distinguish authored thumb/open from absence. GP8 omits both fingerings.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Export existing left and right hand fingerings](https://github.com/CaliLuke/go-guitar-pro/issues/82).

### hammer: Hammer-on and pull-off endpoints

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

Hammer origin is modeled, but explicit destination distinction and full connection identity are projected.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve explicit hammer and pull-off destination markers](https://github.com/CaliLuke/go-guitar-pro/issues/84).

### harmonics: Natural, artificial and feedback harmonics

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Harmonic kinds and fractional fret survive. GP8 reports spelling/octave and conflicting compatibility fret fields.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Resolve harmonic spelling and octave target limits](https://github.com/CaliLuke/go-guitar-pro/issues/96).

### ornaments: Turns and mordents

Notes. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF Note.Ornament is absent. All four upstream turn/mordent variants lack a public representation.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve turns and mordents through GPIF and GP8](https://github.com/CaliLuke/go-guitar-pro/issues/87).

### palm-let-ring: Palm mute and let ring

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Note-level palm mute and let-ring flags survive. Derived duration links are outside the authored model.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### slides: Slides including pick scrapes

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Named slide flags, including pick up/down, have public enum and independent export evidence.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### sound-duration: Authored note duration percentage

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **missing**.

Legacy DurationPercent parses and validates, but GP8 emits no independent duration percentage.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Determine GP8 support for authored note-duration percentages](https://github.com/CaliLuke/go-guitar-pro/issues/102).

### staccato: Staccato

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Boolean staccato survives independent technique checks.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### tapping: Left-hand tapping identity

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

NoteEffect.Hammer, Tapped, and LeftHandTapped preserve three independent source properties and GP8 writes each one independently. HopoDestination remains lossy because the public model has no authored destination field.

Completion criterion: Keep Hammer, Tapped, and LeftHandTapped independent through GPIF import, model edits, and GP8 output while retaining the narrow HopoDestination diagnostic.

### tenuto: Tenuto accent

Notes. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

NoteAccentTenuto is preserved and exports the exact 0x10 GPIF flag under the typed accent authority policy. AlphaTab has no separate staccatissimo note field.

Completion criterion: Keep tenuto distinct through GPIF import, typed model edits, exact 0x10 GP8 output, and pinned AlphaTab consumption; do not infer staccatissimo support.

### trill: Trill fret and speed

Notes. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Fret and duration parse. GP8 preserves fret but reports non-target trill durations.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Determine supported GP8 trill-speed encoding](https://github.com/CaliLuke/go-guitar-pro/issues/103).

### automation-detail: Automation interpolation and annotations

Playback data. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Tempo and sound records preserve order, interpolation, text and visibility in the public model and GPIF wire. The pinned consumer retains tempo detail and sound interpolation/text, but drops hidden visibility from the instrument automation derived from a Sound record; GP8 preflight reports that exact omission. Sync-point occurrence remains separately modeled on SyncPoint.

Completion criterion: Supported import/model scope has non-default public assertions. GP8 export is partial only for the pinned consumer's sound-visibility loss, with exact preflight and independent-consumer evidence.

Bounded work: [Preserve authored tempo and sound automation details](https://github.com/CaliLuke/go-guitar-pro/issues/41).

### backing-track: Embedded audio backing tracks

Playback data. Priority 1. Formats: gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

GP8 embeds enabled Local backing-track metadata and exact audio bytes. Disabled and non-local records remain explicit target omissions.

Completion criterion: Enabled Local assets have exact public-model, GPIF, archive-byte, strict-policy, and pinned AlphaTab evidence. Missing identity/path/data, reserved archive paths, and out-of-range frame padding are rejected before output.

Bounded work: [Export embedded audio backing-track assets](https://github.com/CaliLuke/go-guitar-pro/issues/42).

### midi-bank: MIDI bank selection and bank changes

Playback data. Priority 1. Formats: gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

GP5 track banks and GPIF sound MSB/LSB definitions map to a checked 14-bit public value. GP8 export preserves initial selection and ordered bank/program changes.

Completion criterion: Banks 0, 77, 256, and 16383, exact MSB/LSB wire values, duplicate-position change order, authority, fallback, and invalid ranges are covered by public, wire, reimport, and pinned AlphaTab evidence.

Bounded work: [Preserve MIDI bank selection and bank changes](https://github.com/CaliLuke/go-guitar-pro/issues/51).

### midi-routing: MIDI program, channels and mixer

Playback data. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Selected program, channels, volume, balance and mute/solo are retained. Conflicting ports and mute+solo require target normalization.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Exclude unused legacy MIDI program slots from export rejection](https://github.com/CaliLuke/go-guitar-pro/issues/63); [Classify conflicting MIDI port and playback-state limits](https://github.com/CaliLuke/go-guitar-pro/issues/66).

### pan-automation: Pan and balance automation events

Playback data. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy mix-table balance changes parse. There is no general pan automation collection corresponding to volume events. GP8 omits beat-local changes.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve pan automation independently from static balance](https://github.com/CaliLuke/go-guitar-pro/issues/52).

### sound-automation: Sound definitions and program changes

Playback data. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Ordered sound definitions and references are modeled. GPIF bank selections and legacy beat-local mix changes are not lossless.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve authored opening sound-event preroll](https://github.com/CaliLuke/go-guitar-pro/issues/64); [Prove sound identity independently of MIDI program](https://github.com/CaliLuke/go-guitar-pro/issues/69).

### sustain-pedal: Sustain pedal markers

Playback data. Priority 1. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

AlphaTab parses SustainPedal automation into bar markers. The Go model and writer have no pedal collection.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve sustain pedal down, hold and release markers](https://github.com/CaliLuke/go-guitar-pro/issues/54).

### sync-points: Audio synchronization points

Playback data. Priority 1. Formats: gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **missing**.

Authored bar occurrence, frame offsets, tempo and visibility are modeled. GP8 omits sync points.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Export authored backing-track synchronization points](https://github.com/CaliLuke/go-guitar-pro/issues/55).

### tempo: Tempo values, event order and names

Playback data. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Fractional opening tempo and ordered tempo events survive. HideTempo cannot be emitted. Interpolation and event annotations are tracked separately.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Apply authored HideTempo to the opening GP8 tempo event](https://github.com/CaliLuke/go-guitar-pro/issues/70).

### volume-automation: Volume automation events

Playback data. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **missing**.

Song.VolumeAutomations preserves channel-strip volume events and linear flags. GP8 writer reports them as omitted.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Export authored channel-strip volume automation](https://github.com/CaliLuke/go-guitar-pro/issues/59).

### mix-table: Legacy mix-table changes and effects

Playback data. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **missing**.

Legacy instrument, tempo and controller changes are modeled. Beat-local mix tables are omitted by GP8 export.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Project supported legacy mix-table events into GP8 automation](https://github.com/CaliLuke/go-guitar-pro/issues/85).

### rse: RSE settings and proprietary effects

Playback data. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **missing**.

Legacy RSE records and some GPIF engine/sound state parse. GP8 omits legacy RSE master/track records. Proprietary synthesis is not implemented.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Bound RSE data preservation against real GP8 consumers](https://github.com/CaliLuke/go-guitar-pro/issues/101).

### bounds: Checked boundaries and malformed input

Reliability. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Numeric bounds, mutation cases and fuzz checks cover the public graph. This is evidence of tested classes, not a total correctness claim.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### strict-policy: Parse diagnostics and strict export policy

Reliability. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Unknown/lossy source diagnostics and read-only preflight expose many losses. Silent distinctions still need specific probes.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### alternate-endings: Alternate endings

Structure. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Eight ending bits survive import and export. This represents notation, not an expanded playback sequence.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### directions: Navigation targets and jumps

Structure. Priority 1. Formats: gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

MeasureHeader.Directions preserves the complete canonical marker set from GP5 and GPIF. The legacy Direction pointer has a documented post-parse edit contract, and GP8 emits every target and jump.

Completion criterion: Public, exact-wire, GP8 reimport, malformed-source, post-parse edit, and pinned AlphaTab checks cover all 19 markers and simultaneous sets.

Bounded work: [Preserve navigation targets and jumps on each measure](https://github.com/CaliLuke/go-guitar-pro/issues/45).

### fermata: Fermata placement, kind and length

Structure. Priority 1. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

MeasureHeader.Fermatas preserves exact GPIF offsets, all three symbol types and finite lengths. GP8 retains the authored rational XML, but pinned AlphaTab can move offsets and collapse two distinct holds into one. Strict export currently reports no loss for these cases; #47 remains open for a precise consumer-limit policy.

Completion criterion: Keep exact-offset import, post-parse edits, GP8 wire values, Go reimport and malformed-value diagnostics covered. Add pinned-consumer movement/collision evidence and precise strict-export reporting for affected offsets before claiming full export support.

Bounded work: [Preserve fermata position, type and length](https://github.com/CaliLuke/go-guitar-pro/issues/47).

### free-time: Free-time bars

Structure. Priority 1. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

MeasureHeader.FreeTime preserves exact GPIF master-bar presence independently from numeric meter and GP8 exports the marker without loss.

Completion criterion: Covered by TestConformanceFreeTime and TestAlphaTabPreservesFreeTime, including fixture locations, meter-driven timing, post-parse edits, and presence semantics.

Bounded work: [Preserve free-time bars without inventing a meter](https://github.com/CaliLuke/go-guitar-pro/issues/48).

### key: Key signatures and key mode spelling

Structure. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

GPIF accepts the exact Major, major, Minor, and minor spellings plus the empty major default. Nonzero accidental counts and adjacent changes survive the public model and canonical GP8 Major/Minor export. Unknown modes remain permissive-major with an explicit unsupported-feature diagnostic and strict rejection.

Completion criterion: Covered by focused public API, exact wire, reimport, diagnostic-policy, and pinned AlphaTab key-mode evidence.

Bounded work: [Preserve lowercase GPIF minor key modes](https://github.com/CaliLuke/go-guitar-pro/issues/49).

### meter: Meter numerator and denominator

Structure. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Meter values survive within the checked public bounds. Numerator is limited to 127.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### ownership: Multiple tracks, staves and voices

Structure. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Independent occurrences, staff order, voice holes and staff-0 compatibility authority have focused tests.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### repeat-count: Repeat starts and displayed pass counts

Structure. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Current API uses total passes: no end=0, ordinary repeat=2, displayed 6x=6. Supported maximum is 128. Issue 38 describes the old API.

Completion criterion: Keep direct public values for 0, 1, 2, 6, and 128 across applicable codecs and the independent consumer.

### simile: One-bar and two-bar simile marks

Structure. Priority 1. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

GPIF simile marks now preserve None, Simple, FirstOfDouble and SecondOfDouble through Measure.SimileMark and GP8 export. Commit 339113c adds exact import, independent AlphaTab export and invalid-value evidence. This represents authored repeat symbols; it does not expand playback traversal.

Completion criterion: Maintain all four values at the owning measure/staff, exact independent GP8 export, and unknown-source/undefined-authored-value checks.

### double-bar: Double bar lines

Structure. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Ordinary double bars are emitted. AlphaTab GPIF import clears the final bar DoubleBar flag, producing 12 binary-to-GP8 consumer differences. The wire field alone does not prove final-bar parity.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Resolve final-bar double-line consumer differences](https://github.com/CaliLuke/go-guitar-pro/issues/95).

### sections: Separate section marker and text

Structure. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **partial**. Model: **partial**. GP8 export: **partial**.

GPIF Letter and Text collapse into Marker.Title. A section containing both loses its separate letter. Export writes text.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Keep section letters separate from section text](https://github.com/CaliLuke/go-guitar-pro/issues/90).

### barlines: Extended barline and bar-number controls

Structure. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

Only double bars have a dedicated public contract. Dashed/heavy/tick/custom bar lines and per-bar number display are absent. Some upstream values originate in other formats.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve global extended barlines and bar-number policy](https://github.com/CaliLuke/go-guitar-pro/issues/109).

### common-time: Common-time and cut-time notation

Structure. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

Public TimeSignature contains numeric meter and beam groups, but no common-time display flag. Pinned GPIF import does not expose a dedicated common-time case.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Determine Guitar Pro support for common and cut time](https://github.com/CaliLuke/go-guitar-pro/issues/110).

### beat-lyrics: Lyrics authored directly on a beat

Text. Priority 2. Formats: gp6, gp7, gp8. Scope: guitar-pro.

Import: **missing**. Model: **missing**. GP8 export: **missing**.

GPIF Beat.Lyrics has no dedicated public destination. FreeText and track lyrics cannot preserve this source scope.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Preserve lyrics authored directly on beats](https://github.com/CaliLuke/go-guitar-pro/issues/76).

### lyrics: Score and track lyrics

Text. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Ordered score/track lyric text and offsets parse. GP8 preserves track lyrics but omits binary score lyrics. Syllable dispatch is not a rendering service here.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Export assigned legacy score lyrics as GPIF track lyrics](https://github.com/CaliLuke/go-guitar-pro/issues/98).

### text: Beat text including rests

Text. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Free text survives on both note beats and rests.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### duration: Note values and dots

Timing. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Public duration values, single/double dots and checked export limits have focused cases. Conflicting dot flags produce normalization.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### pickup: Pickup bars and exact authored timing

Timing. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Exact rational timing and anacrusis are modeled and tested. This does not implement AlphaTab performance timing.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### tuplets: Exact tuplet ratios

Timing. Priority 1. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

Authored n:m ratios are exact within the public legacy bounds. Zero:zero is a reported canonicalization on export.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### rests: Rests and explicit empty beats

Timing. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **partial**.

Rests survive. An explicit empty beat becomes a rest on GP8 export with a normalization report.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

Bounded work: [Establish whether GP8 can preserve explicit empty beats](https://github.com/CaliLuke/go-guitar-pro/issues/100).

### triplet-feel: Triplet, dotted and Scottish feel

Timing. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: guitar-pro.

Import: **supported**. Model: **supported**. GP8 export: **supported**.

All seven public triplet-feel variants have import and export evidence.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### other-exports: AlphaTex and other export formats

Other AlphaTab services. Priority 3. Formats: alphatex. Scope: excluded.

Import: **out-of-scope**. Model: **out-of-scope**. GP8 export: **out-of-scope**.

The module exports GP8. AlphaTex and non-GP8 output are separate product scope.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### other-imports: MusicXML, Capella and AlphaTex import

Other AlphaTab services. Priority 3. Formats: musicxml, capella, alphatex. Scope: excluded.

Import: **out-of-scope**. Model: **out-of-scope**. GP8 export: **out-of-scope**.

These are AlphaTab importers outside this Guitar Pro library scope. Keep them visible without counting them as Guitar Pro gaps.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### rendering: Engraving, layout engine and font rendering

Other AlphaTab services. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: excluded.

Import: **out-of-scope**. Model: **out-of-scope**. GP8 export: **out-of-scope**.

Rendering engines and style computation are not library goals. Authored Guitar Pro display data remains separately inventoried.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### serialization: AlphaTab JSON model and object graph utilities

Other AlphaTab services. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: excluded.

Import: **out-of-scope**. Model: **out-of-scope**. GP8 export: **out-of-scope**.

AlphaTab-specific IDs, object backreferences, lookup caches, JSON conversion and derived chain links are outside the public compatibility API. Authored ties and slurs have separate rows.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### synthesis: Synthesizer, MIDI generation and audio player

Other AlphaTab services. Priority 3. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: excluded.

Import: **out-of-scope**. Model: **out-of-scope**. GP8 export: **out-of-scope**.

SoundFont synthesis, real-time player controls and MIDI generation are outside the documented authored-score scope.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.

### repeat-traversal: Expanded repeat and jump traversal

Playback engine. Priority 2. Formats: gp3, gp4, gp5, gp6, gp7, gp8. Scope: excluded.

Import: **out-of-scope**. Model: **out-of-scope**. GP8 export: **out-of-scope**.

The library stores authored order. AlphaTab repeat groups and MIDI traversal are derived playback services outside the documented package scope.

Completion criterion: Add non-default public API assertions for the remaining variants and compare GP8 output with pinned AlphaTab. Classify each loss explicitly.
